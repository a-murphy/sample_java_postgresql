language: java

jdk:
 - oraclejdk7
 - openjdk6
 - openjdk7
env:
 - FOO=foo BAR=bar
 - FOO=bar BAR=foo
script:
 - gradle clean build
 - gradle test
 - gradle cobertura

parallelized_test: true

notifications:
 email: false

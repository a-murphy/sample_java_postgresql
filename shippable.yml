language: java

jdk:
 - openjdk7
 
script:
 - gradle clean build
 - gradle test
 - gradle cobertura

parallelized_test: true

notifications:
 email: false
 irc:
  pull_requests: true
  channels:
    - localhost#channel

language: java

jdk:
 - openjdk7
 
script:
 - gradle clean build
 - gradle test
 - gradle cobertura

parallelized_test: true

notifications:
 email: 
   on_success: never
   on_failure: never
   recipients: aidan@shippable.com

archive: 'true'
